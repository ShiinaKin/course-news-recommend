<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.sakurasou.newsrecommend.dao.UserEventDAO">

  <insert id="insert" parameterType="io.sakurasou.newsrecommend.model.UserEvent"
          useGeneratedKeys="true" keyProperty="id">
    INSERT INTO user_events (user_id, article_id, event_type, created_at)
    VALUES (#{userId}, #{articleId}, #{eventType}, #{createdAt})
  </insert>

  <select id="findRecentArticleIdsByUser" resultType="long">
    SELECT article_id
    FROM user_events
    WHERE user_id = #{userId}
    ORDER BY created_at DESC
    LIMIT #{limit}
  </select>

</mapper>
