<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.sakurasou.newsrecommend.dao.MediaJobDAO">

  <resultMap id="MediaJobResult" type="io.sakurasou.newsrecommend.model.MediaJob">
    <id column="id" property="id"/>
    <result column="user_id" property="userId"/>
    <result column="type" property="type"/>
    <result column="file_path" property="filePath"/>
    <result column="status" property="status"/>
    <result column="result_text" property="resultText"/>
    <result column="created_at" property="createdAt"/>
    <result column="updated_at" property="updatedAt"/>
  </resultMap>

  <select id="findById" parameterType="long" resultMap="MediaJobResult">
    SELECT id, user_id, type, file_path, status, result_text, created_at, updated_at
    FROM media_jobs
    WHERE id = #{id}
  </select>

  <insert id="insert" parameterType="io.sakurasou.newsrecommend.model.MediaJob"
          useGeneratedKeys="true" keyProperty="id">
    INSERT INTO media_jobs (user_id, type, file_path, status, result_text, created_at, updated_at)
    VALUES (#{userId}, #{type}, #{filePath}, #{status}, #{resultText}, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP)
  </insert>

  <update id="updateRunning">
    UPDATE media_jobs
    SET status = #{status},
        updated_at = CURRENT_TIMESTAMP
    WHERE id = #{id}
  </update>

  <update id="updateStatus">
    UPDATE media_jobs
    SET status = #{status},
        result_text = #{resultText},
        updated_at = CURRENT_TIMESTAMP
    WHERE id = #{id}
  </update>

</mapper>
